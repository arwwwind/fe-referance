import { fromJS } from 'immutable';

export default fromJS({
  caseId: null,
  serviceId: null,
  redDot: null,
  lastUpdate: null,
  nextUpdate: null,
  daysOverdue: null,
  loading: false,
  error: null
});
